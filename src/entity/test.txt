array<BufferedImage> = [[dw0, dw1, dw2], 
                        [up1, up2, up3], 
                        [sx0, sx1, sx2], 
                        [dx0, dx1, dx2]]

dw = [0].get(spriteNum)
dx = [1][]
sx = [2][spriteNum]
up = [3][spriteNum]



int maxSprite = 3;  // quanti sprite usa per esempio per muoversi a sx/dx/up/dw

int spriteNum = 0 -> maxSprite-1 (2)
direzione = dw
non fa un cazzo

direzione = dx
spriteNum += maxSprite quindi spriteNum = 

direzione = Swing
int spriteNum = (0 -> maxSprite-1)

------

int maxSprite = 3;
int spriteNum = 0;

spriteCounter++;
if(spriteCounter > 15){  // ogni 15/60 volte al secondo 
    spriteNum++;
    if(spriteNum = maxSprite-1)  // se arriva al massimo degli sprite
        spriteNum = 0;
    spriteCounter = 0;  // e resetta il counter
}

switch (direction)
    case dw:
        array[0][sprirteNum];
    case up:
        array[1][spriteNum];
    case sx:
        array[2][spriteNum];
    case dx:
        array[3][sprirteNum];

------

TEST FORSE PIU VELOCE
[dw1, dw2, dw3, up1, up2, up3, sx1, sx2, sx3, dx1, dx2, dx3]

spriteCounter++;
if(spriteCounter > 15){  // ogni 15/60 volte al secondo 
    spriteNum++;
    if(spriteNum = maxSprite-1)  // se arriva al massimo degli sprite
        spriteNum = 0;
    spriteCounter = 0;  // e resetta il counter
}
switch (direction)
    case dw:
        spriteNum += maxSprite*0 // per prendere le prime 3 sprite [0:2]
    case up:
        spriteNum += maxSprite*1 // per prendere le sprite [3:5]
    case sx:
        spriteNum += maxSprite*1 // per prendere le sprite [3:5]
    case dx:
        spriteNum += maxSprite*1 // per prendere le sprite [3:5]

spriteNum += maxSprite*0 // per prendere le prime 3 sprite [0:2]

spriteNum += maxSprite*1 // per prendere le sprite [3:5]
